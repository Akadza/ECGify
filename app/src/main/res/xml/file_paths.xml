<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Разрешаем доступ к cacheDir (временные фото) -->
    <cache-path name="camera_photos" path="." />

    <!-- Опционально: external-cache-path для внешнего кэша -->
    <external-cache-path name="camera_photos_external" path="." />
</paths>